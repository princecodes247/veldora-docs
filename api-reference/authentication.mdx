---
title: "Authentication"
description: "Example overview page before API endpoints"
---

Use the Veldora Secret Key available in your Bucket Settings as Bearer token in the Authorization header:

```bash
'Authorization': 'Token <team_token>'
```
