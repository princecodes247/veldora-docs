---
title: "Rate Limits"
description: "Veldora uses bearer token to validate api request."
---

By default, the API endpoints can be use without API key. If you wants to secure the API then you can Enable API Key Security in your Bucket Settings as Bearer token in the Authorization header:

```bash
'Authorization': 'Token <team_token>'
```
